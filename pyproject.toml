[project]
name = "order-kafka-poc"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "shared",
    "order-api",
    "inventory-service",
    "payment-service",
    "shipping-service",
]
